@model IEnumerable<CPICPP.Models.TestSession>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserResponsesJson)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuestionsAskedJson)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GeneratedMBTIType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Score)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TestName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserResponsesJson)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuestionsAskedJson)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GeneratedMBTIType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Score)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TestName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TestSessionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TestSessionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TestSessionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
